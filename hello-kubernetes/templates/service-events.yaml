apiVersion: v1
kind: Service
metadata:
  name: events-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: events-service
  ports:
    - port: {{ .Values.eventsService.port }}
      targetPort: {{ .Values.eventsService.port }}
